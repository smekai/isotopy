---
name: run-app
description: Launch and drive the Isotopy app (Hono server :9477 + Vite UI :5173) — dev command, health checks, headless-Chromium driving, and the sandbox gotcha for real engine runs
---

# Running the Isotopy app

The instructions live in [`docs/running-the-app.md`](../../../docs/running-the-app.md).
Read that file before acting on this skill.

Two harnesses discover skills at different paths — `.claude/skills/` for Claude
Code, `.agents/skills/` for Codex — so this file exists twice. The body lives in
`docs/` so there is one copy to maintain rather than two that drift apart.
