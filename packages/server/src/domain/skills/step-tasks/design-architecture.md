# Assignment: Design the software architecture

Decide whether the approved feature is cross-cutting, data-heavy,
infrastructure-related, security-sensitive, or platform-specific. If focused
implementation can follow existing patterns without an architecture decision,
explain why and end with exactly `VERDICT: SKIP`.

Otherwise provide a proportional implementation design covering boundaries,
interfaces, data flow, persistence or migrations, failure handling, security,
compatibility, validation points, and Windows/macOS behaviour. Do not write
production code or expand product scope.

End with exactly `VERDICT: PASS` when the implementation design is complete,
otherwise `VERDICT: FAIL`.
