---
name: create-pr
description: Use when opening a pull request, preparing a branch for review, or writing a PR description — requires a mandatory table of lines changed per category (source, tests, task board, run evidence, docs), forces an answer when prose outweighs code, and keeps preparation material out of the PR
---

# Opening a pull request

The instructions live in [`docs/creating-a-pr.md`](../../../docs/creating-a-pr.md).
Read that file before acting on this skill.

Two harnesses discover skills at different paths — `.claude/skills/` for Claude
Code, `.agents/skills/` for Codex — so this file exists twice. The body lives in
`docs/` so there is one copy to maintain rather than two that drift apart.
